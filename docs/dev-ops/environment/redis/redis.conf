# Redis server configuration for the local Docker environment.
bind 0.0.0.0
protected-mode yes
port 6379
tcp-keepalive 300
appendonly yes

# Disable the implicit unauthenticated account and use the application account.
user default off
user qijiv on >18903705503qQ ~* &* +@all
