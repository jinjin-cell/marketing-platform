<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.qijiv.infrastructure.persistent.dao.IRaffleActivityAccountMonthDao">

    <resultMap id="RaffleActivityAccountMonthMap" type="cn.qijiv.infrastructure.persistent.po.RaffleActivityAccountMonthPO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="activity_id" property="activityId"/>
        <result column="month" property="month"/>
        <result column="month_count" property="monthCount"/>
        <result column="month_count_surplus" property="monthCountSurplus"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="cn.qijiv.infrastructure.persistent.po.RaffleActivityAccountMonthPO">
        INSERT INTO raffle_activity_account_month
            (user_id, activity_id, month, month_count, month_count_surplus)
        VALUES
            (#{userId}, #{activityId}, #{month}, #{monthCount}, #{monthCountSurplus})
    </insert>

    <select id="queryRaffleActivityAccountMonth" resultMap="RaffleActivityAccountMonthMap">
        SELECT id, user_id, activity_id, month, month_count, month_count_surplus, create_time, update_time
        FROM raffle_activity_account_month
        WHERE user_id = #{userId}
          AND activity_id = #{activityId}
          AND month = #{month}
    </select>

    <update id="updateAccountQuota"
            parameterType="cn.qijiv.infrastructure.persistent.po.RaffleActivityAccountMonthPO">
        UPDATE raffle_activity_account_month
        SET month_count = month_count + #{monthCount},
            month_count_surplus = month_count_surplus + #{monthCountSurplus},
            update_time = NOW()
        WHERE user_id = #{userId}
          AND activity_id = #{activityId}
          AND month = #{month}
    </update>

</mapper>
