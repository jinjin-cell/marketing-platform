<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.qijiv.infrastructure.persistent.dao.IRaffleActivityOrderDao">

    <resultMap id="RaffleActivityOrderMap" type="cn.qijiv.infrastructure.persistent.po.RaffleActivityOrderPO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="sku" property="sku"/>
        <result column="activity_id" property="activityId"/>
        <result column="activity_name" property="activityName"/>
        <result column="strategy_id" property="strategyId"/>
        <result column="order_id" property="orderId"/>
        <result column="out_business_no" property="outBusinessNo"/>
        <result column="order_time" property="orderTime"/>
        <result column="total_count" property="totalCount"/>
        <result column="day_count" property="dayCount"/>
        <result column="month_count" property="monthCount"/>
        <result column="pay_amount" property="payAmount"/>
        <result column="state" property="state"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="cn.qijiv.infrastructure.persistent.po.RaffleActivityOrderPO">
        INSERT INTO raffle_activity_order
            (user_id, sku, activity_id, activity_name, strategy_id, order_id, out_business_no, order_time, pay_amount,
             total_count, day_count, month_count, state)
        VALUES
            (#{userId}, #{sku}, #{activityId}, #{activityName}, #{strategyId}, #{orderId}, #{outBusinessNo}, #{orderTime},
             #{payAmount},#{totalCount}, #{dayCount}, #{monthCount}, #{state})
    </insert>

    <select id="queryRaffleActivityOrderByUserId" resultMap="RaffleActivityOrderMap">
        SELECT id, user_id, sku, activity_id, activity_name, strategy_id, order_id, out_business_no, order_time,pay_amount,
               total_count, day_count, month_count, state, create_time, update_time
        FROM raffle_activity_order
        WHERE user_id = #{userId}
        ORDER BY id DESC
    </select>

    <select id="queryByOutBusinessNo" resultMap="RaffleActivityOrderMap">
        SELECT user_id, sku, activity_id, activity_name, strategy_id, order_id, out_business_no, order_time, pay_amount,
               total_count, day_count, month_count, state
        FROM raffle_activity_order
        WHERE user_id = #{userId}
          AND out_business_no = #{outBusinessNo}
        LIMIT 1
    </select>

    <!-- 启动时由 ShardingSphere 广播到全部订单分表，用于构建无假阴性的 Bloom 超集。 -->
    <select id="queryAllBusinessKeys" resultMap="RaffleActivityOrderMap">
        SELECT user_id, out_business_no
        FROM raffle_activity_order
    </select>

    <select id="queryRaffleActivityOrder" parameterType="cn.qijiv.infrastructure.persistent.po.RaffleActivityOrderPO" resultMap="RaffleActivityOrderMap">
        SELECT id, user_id, sku, activity_id, activity_name, strategy_id, order_id, out_business_no, order_time, pay_amount,
               total_count, day_count, month_count, state
        FROM raffle_activity_order
        WHERE user_id = #{userId}
          AND out_business_no = #{outBusinessNo}
        LIMIT 1
    </select>

    <update id="updateOrderCompleted" parameterType="cn.qijiv.infrastructure.persistent.po.RaffleActivityOrderPO">
        UPDATE raffle_activity_order
        SET state = 'completed',
            update_time = NOW()
        WHERE user_id = #{userId}
          AND out_business_no = #{outBusinessNo}
          AND state = 'wait_pay'
    </update>

    <select id="queryUnpaidActivityOrder" parameterType="cn.qijiv.infrastructure.persistent.po.RaffleActivityOrderPO" resultMap="RaffleActivityOrderMap">
        select user_id, order_id, out_business_no, pay_amount
        from raffle_activity_order
        where user_id = #{userId} and sku = #{sku} and state = 'wait_pay' and order_time >= NOW() - INTERVAL 1 MONTH
        limit 1
    </select>

    <!-- 不带 user_id 分片键，由 ShardingSphere 广播到全部分表，将超过一个月未支付的订单置为过期 -->
    <update id="updateOrderExpired">
        UPDATE raffle_activity_order
        SET state = 'expired',
            update_time = NOW()
        WHERE state = 'wait_pay'
          AND order_time &lt; #{beforeTime}
    </update>

</mapper>
