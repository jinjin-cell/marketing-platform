<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.qijiv.infrastructure.persistent.dao.IRaffleActivitySkuDao">

    <resultMap id="RaffleActivitySkuMap" type="cn.qijiv.infrastructure.persistent.po.RaffleActivitySkuPO">
        <id column="id" property="id"/>
        <result column="sku" property="sku"/>
        <result column="activity_id" property="activityId"/>
        <result column="activity_count_id" property="activityCountId"/>
        <result column="stock_count" property="stockCount"/>
        <result column="stock_count_surplus" property="stockCountSurplus"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!--suppress SqlResolve -->
    <select id="queryRaffleActivitySkuBySku" resultMap="RaffleActivitySkuMap">
        SELECT id, sku, activity_id, activity_count_id, stock_count, stock_count_surplus,
               create_time, update_time
        FROM raffle_activity_sku
        WHERE sku = #{sku}
    </select>
    <!--suppress SqlResolve -->
    <select id="queryActivitySkuListByActivityId" resultMap="RaffleActivitySkuMap">
        SELECT id, sku, activity_id, activity_count_id, stock_count, stock_count_surplus,
               create_time, update_time
        FROM raffle_activity_sku
        WHERE activity_id = #{activityId}
    </select>

    <!--suppress SqlResolve -->
    <update id="updateActivitySkuStock">
        UPDATE raffle_activity_sku
        SET stock_count_surplus = stock_count_surplus - 1,
            update_time = NOW()
        WHERE sku = #{sku}
          AND stock_count_surplus &gt; 0
    </update>

    <!--suppress SqlResolve -->
    <update id="clearActivitySkuStock">
        UPDATE raffle_activity_sku
        SET stock_count_surplus = 0,
            update_time = NOW()
        WHERE sku = #{sku}
    </update>

</mapper>
