<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.qijiv.infrastructure.persistent.dao.IRuleTreeNodeDao">

    <resultMap id="RuleTreeNodeMap" type="cn.qijiv.infrastructure.persistent.po.RuleTreeNodePO">
        <id column="id" property="id"/>
        <result column="tree_id" property="treeId"/>
        <result column="rule_key" property="ruleKey"/>
        <result column="rule_desc" property="ruleDesc"/>
        <result column="rule_value" property="ruleValue"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryRuleTreeNodeListByTreeId" parameterType="java.lang.String" resultMap="RuleTreeNodeMap">
        SELECT id, tree_id, rule_key, rule_desc, rule_value, create_time, update_time
        FROM rule_tree_node
        WHERE tree_id = #{treeId}
        ORDER BY id
    </select>

    <select id="queryRuleLockNodeListByTreeIds" resultMap="RuleTreeNodeMap">
        SELECT id, tree_id, rule_key, rule_desc, rule_value, create_time, update_time
        FROM rule_tree_node
        WHERE rule_key = 'rule_lock'
          AND tree_id IN
        <foreach collection="treeIds" item="treeId" open="(" separator="," close=")">
            #{treeId}
        </foreach>
    </select>

</mapper>
